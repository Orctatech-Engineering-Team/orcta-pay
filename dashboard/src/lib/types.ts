export type ChargeRow = { ref: string; product: string; gateway: string; amount_pesewas: number; currency: string; status: "pending" | "succeeded" | "failed"; external_ref?: string; created_at: string; gateway_event?: { raw_request: unknown; raw_response: unknown } };
export type PayoutLine = { vendor_id: string; vendor_name: string; amount_pesewas: number; net_pesewas: number; recipient: string; reservation_status: "open" | "settled" | "released"; reservation_created_at: string; reservation_resolved_at?: string };
export type PayoutBatchRow = { id: string; batch_date: string; status: "running" | "completed" | "partially_failed"; vendor_count: number; gross_pesewas: number; commission_pesewas: number; net_pesewas: number; created_at: string; completed_at?: string; lines: PayoutLine[] };
export type LedgerEntry = { id: string; product: string; wallet: string; vendor_id: string; entry_type: "credit" | "debit"; amount_pesewas: number; reason: "order_payment" | "commission_deduction" | "payout" | "adjustment"; ref: string; value_time: string; booking_time: string; settlement_time: string | null; kind: "vendor" | "commission" };
export type GatewayHealthRow = { gateway: string; channel: string; eligible: boolean; rolling_success_rate: number; p95_latency_ms: number; circuit_state: "closed" | "open" | "half_open"; cost_bps: number; cost_fixed_pesewas: number; rank: number; open_since?: string };
export type WebhookRow = { id: string; aggregator_event_id: string; gateway: string; kind: string; payload: unknown; received_at: string; processed_at: string | null };
export type AppRow = { id: string; name: string; prefix: string; created_at: string; last_used_at: string | null; revoked: boolean };
